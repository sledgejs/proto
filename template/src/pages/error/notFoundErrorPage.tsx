import { observer } from 'mobx-react-lite';

export const NotFoundErrorPage = observer(() => {
  return (
    <div className="not-found-error-page error-page">
      Not found
    </div>
  );
});

export default NotFoundErrorPage;