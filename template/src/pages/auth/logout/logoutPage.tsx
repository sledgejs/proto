import { observer } from 'mobx-react-lite';

type Props = {

}

export const LogoutPage = observer((props: Props) => {

  return (
    <div className="logout-page">

    </div>
  );
});

export default LogoutPage;